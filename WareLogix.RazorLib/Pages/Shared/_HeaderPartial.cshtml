@model WareLogix.RazorLib.Models.HeaderData
<nav class="light-blue lighten-1" role="navigation">
    <div class="nav-wrapper container">
        <a id="logo-container" href="@Model.BrandLink.Href" class="brand-logo">@Model.BrandLink.DisplayText</a>
        <ul class="right hide-on-med-and-down">
            @foreach (var item in Model.NavbarLinks)
            {
                <li><a href="@item.Href">@item.DisplayText</a></li>
            }
        </ul>

        <ul id="nav-mobile" class="sidenav">
            @foreach (var item in Model.NavbarLinks)
            {
                <li><a href="@item.Href">@item.DisplayText</a></li>
            }
        </ul>
        <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    </div>
</nav>
